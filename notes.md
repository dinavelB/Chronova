Custom decorators meaning

1. @layer base - For base/reset styles (like your html, body styles)

2. @layer components - For component classes (buttons, cards, forms, etc.)

3. @layer utilities - For utility classes (margin, padding, colors, etc.)
